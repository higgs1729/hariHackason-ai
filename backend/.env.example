# Copy to .env and fill in. .env is git-ignored; never commit real values.
# Names only here — see docs/design/03-detailed-design.md section 6.

# Anthropic API key for album generation (slice 6).
ANTHROPIC_API_KEY=

# Signing secret for access/refresh tokens (slice 2). Use 32+ random bytes.
APP_JWT_SECRET=
