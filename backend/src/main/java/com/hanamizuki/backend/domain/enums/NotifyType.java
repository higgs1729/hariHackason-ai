package com.hanamizuki.backend.domain.enums;

/** {@code notification.notifyType}. Stored as the constant name. */
public enum NotifyType {
    FRIEND_REQUEST, FRIEND_ACCEPTED, ALBUM_INVITED,
    ALBUM_UPDATED, CAPSULE_OPENABLE, SHARE_VIEWED
}
